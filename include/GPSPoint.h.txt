#ifndef GPSPOINT_H
#define GPSPOINT_H

#include <string>

// 基础数据结构：确保全组数据定义统一
struct GPSPoint {
    int taxiID;          // 出租车编号
    long long timestamp; // 时间戳（秒）
    double lon;          // 经度
    double lat;          // 纬度
};

#endif